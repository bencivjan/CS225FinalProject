#include "Graph.h"

Graph::Graph(std::vector<Airport> airports, std::vector<Route> routes){
    int i = 0;
    std::ifstream airport_data;
    std::ifstream route_data;
    while(!airport_data.eof()){

    }

    while(!route_data.eof()){
        
    }
}